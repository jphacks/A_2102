name: a_2102
description: This is a project of JPHACKS2021.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: 0.13.3
  get: ^4.1.4
  cupertino_icons: ^1.0.2
#  firebase_core: ^1.8.0
#  cloud_firestore: ^2.5.4
  flutter_native_splash: ^1.1.5+1

flutter_native_splash:
    image: "./splash.png"
    color: "FFEB3B"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
